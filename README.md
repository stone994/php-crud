# php-crud
This project is a PHP CRUD (Create, Read, Update, Delete) application that demonstrates basic database operations using PHP and MySQL. It provides a clean and user-friendly interface for managing records efficiently.
